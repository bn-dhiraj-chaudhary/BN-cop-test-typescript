capture:
  files:
    exclude-regex: '((node_modules|dist|\.git)/.*)'
analyze:
  cov-analyze-args:
    - '--webapp-security'
    - '--security'
    - '--enable'
    - 'HARDCODED_CREDENTIALS'
    - '--enable'
    - 'SQLI'
    - '--enable'
    - 'OS_CMD_INJECTION'
